

<% @user.errors.full_messages.each do |message| %>
	<% @block =  '#error-field' %>

	var block = $('#error-field')
	
	block.show()
	block.html('<%= j (message)%>').css({"padding": "10px 30px 10px 15px", "border-radius": "8px"}).fadeOut(10000).click(function() {
	$(this).hide();
	});

<% end %>

